train:
  exp_name: i2_03_global_semantic_alignment_only

model:
  fusion_mode: ours
  alignment:
    enabled: false
    penalty_scale: 0.0
    bonus_scale: 0.0
    context_scale: 0.0
  gate:
    mode: fixed

loss:
  semantic_alignment_weight: 0.03
  class_aware_alignment_same_class_weight: 0.25
