base: ../base.yaml

train:
  output_dir: results/aeg_robust/loss/plain_kl

robust:
  train_aug: true

loss:
  mode: plain_kl
  ce_weight: 1.0
  consistency_weight: 0.05
  aug_ce_weight: 0.0