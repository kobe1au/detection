base: ../base.yaml

train:
  output_dir: results/aeg_robust/loss/compact_kl

robust:
  train_aug: true

loss:
  mode: compact_kl
  ce_weight: 1.0
  consistency_weight: 0.05
  aug_ce_weight: 0.0