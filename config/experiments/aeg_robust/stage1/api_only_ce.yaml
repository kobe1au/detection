base: ../base.yaml

train:
  output_dir: results/aeg_robust/stage1/api_only_ce

model:
  masked_node_types:
    - METHOD
    - PERMISSION
    - INTENT
    - COMPONENT
    - RISK_SEMANTIC
    - STRING_HINT
