base: ../base.example.yaml

train:
  output_dir: results/aeg_robust/i2/multiview_contrast

model:
  source_bias_weight: 0.0
  reliability_bias_weight: 0.0
  conflict_bias_weight: 0.0

loss:
  counterfactual_kl_weight: 0.0
