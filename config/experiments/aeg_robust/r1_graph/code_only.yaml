base: ../base.yaml

train:
  output_dir: results/aeg_robust/r1_graph/code_only

model:
  masked_node_types:
    - PERMISSION
    - COMPONENT
    - INTENT
    - RISK_SEMANTIC

loss:
  mode: compact_kl
  consistency_weight: 0.05