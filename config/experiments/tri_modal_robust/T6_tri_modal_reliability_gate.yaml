defaults:
  - base_tri_modal_robust.yaml

train:
  exp_name: "T6_tri_modal_reliability_gate"

model:
  fusion_mode: "tri_modal_reliability_gate"

loss:
  branch_aux_weight: 0.05
  soft_consistency_weight: 0.0
  gate_prior_weight: 0.0
