defaults:
  - base_tri_modal_robust.yaml

train:
  exp_name: "T7_tri_modal_full_no_manifest_aux"

model:
  fusion_mode: "tri_modal_ours"

loss:
  branch_aux_weight: 0.05
  branch_aux_weights:
    api: 1.0
    graph: 1.0
    manifest: 0.0
    joint: 1.0
  soft_consistency_weight: 0.05
  gate_prior_weight: 0.01
