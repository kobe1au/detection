defaults:
  - base_tri_modal_robust.yaml

train:
  exp_name: "T1_graph_only"

model:
  fusion_mode: "graph_only"

loss:
  branch_aux_weight: 0.0
  soft_consistency_weight: 0.0
  gate_prior_weight: 0.0

robust:
  train_aug: false
