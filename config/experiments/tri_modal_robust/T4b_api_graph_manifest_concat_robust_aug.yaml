defaults:
  - base_tri_modal_robust.yaml

train:
  exp_name: "T4b_api_graph_manifest_concat_robust_aug"

model:
  fusion_mode: "tri_modal_concat"

loss:
  branch_aux_weight: 0.0
  soft_consistency_weight: 0.0
  gate_prior_weight: 0.0

robust:
  train_aug: true
